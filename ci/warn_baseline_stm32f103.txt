# Warning baseline for the stm32f103 build (-Wall -Wextra, arm-none-eabi-gcc).
# Format: normalized warnings, see ci/warn_ratchet.py header. Ratchet is
# one-way: only remove lines here manually, after the warning is really gone.
#
# Regenerated from a REAL build (grbl/platform/PLAN.md Phase 1 fixed the
# spindle_control.c / handlers.c build breakage that previously prevented
# arm-none-eabi-gcc from ever reaching this file list). Union of BUILD=DEBUG
# and BUILD=RELEASE logs - both produced the identical warning set. Command:
#   make -C grbl/platform/stm32f103 BUILD=<DEBUG|RELEASE> > build.log 2>&1
#   python3 ci/warn_ratchet.py --log build.log --emit
#
# Removed 2026-07-26: `stepper.c: ... integer overflow ... '25032704' [-Woverflow]`
# (BUG #18 class, CONTRACTS.md's clock-constant-width section). Root cause:
# common/stm32/common.mk's
# F_CPU lacked a suffix wide enough for 72 MHz - the true compile-time
# constant is 4320000000, not 25032704. Fixed at the source (common.mk now
# uses ULL, not the UL that would still have silently wrapped at this
# clock); do NOT re-add - if this warning returns, F_CPU lost its ULL again.
#
# The "config.h vs platform.h dual pin-map canon" entries that used to sit
# here (CONTROL_MASK/COOLANT_FLOOD_PIN/DIRECTION_MASK/LIMIT_MASK/PROBE_MASK/
# SPINDLE_DIRECTION_PIN/SPINDLE_ENABLE_PIN/STEPPERS_DISABLE_MASK/STEP_MASK,
# all "redefined") were BUG #25, not harmless debt: SPINDLE_ENABLE_PIN/
# SPINDLE_DIRECTION_PIN/COOLANT_FLOOD_PIN genuinely disagreed in value
# between the two files, and which value won depended on per-translation-
# unit include order - the spindle enable relay's pin was configured as an
# output on one physical pin and driven (GPIO_BSET/GPIO_BCLR) on a different
# one. Baselining the warning instead of reading it hid a live wiring defect
# behind a green ratchet for as long as the entry sat here - see
# CONTRACTS.md #gpio-pin-map-single-owner. Fixed by making platform.h the
# sole owner of every pin/port/bit/mask; do NOT re-add any of these lines -
# if this class of warning reappears, that means a duplicate pin-map
# definition has come back and the ratchet is now supposed to fail on it.
#
# common/stm32 shared code (regs.h vs CMSIS-style headers, both stm32 ports):
stm32_platform.h: warning: comparison of unsigned expression in '< 0' is always false [-Wtype-limits]
stm32_timing.c: warning: "CoreDebug_DEMCR_TRCENA_Msk" redefined
stm32_timing.c: warning: "DWT_CTRL_CYCCNTENA_Msk" redefined
stm32_watchdog.c: warning: "IWDG_BASE" redefined
#
# stm32f103 platform code:
platform.c: warning: unused parameter 'port' [-Wunused-parameter]
#
# GRBL core (shared with every platform; not stm32f103-specific):
gcode.c: warning: this statement may fall through [-Wimplicit-fallthrough=]
motion_control.c: warning: unused parameter 'cycle_mask' [-Wunused-parameter]
nvmem.c: warning: '<<' in boolean context, did you mean '<'? [-Wint-in-bool-context]
report.c: warning: this statement may fall through [-Wimplicit-fallthrough=]
system.c: warning: this statement may fall through [-Wimplicit-fallthrough=]
