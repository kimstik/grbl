# Warning baseline for the samd21 build (-Wall -Wextra, arm-none-eabi-gcc).
# Shared by both boards (megarm, generic).
# Format: normalized warnings, see ci/warn_ratchet.py header. Ratchet is
# one-way: only remove lines here manually, after the warning is really gone.
#
# Known debt, tracked in grbl/platform/PLAN.md Phase 1. Notes:
#  - "EEPROM_SIZE" redefined: platform/samd21/nvmem.c:20 vs platform.h:52
#  - core grbl/nvmem.c is NOT compiled here (platform override), so no
#    -Wint-in-bool-context entry.
# Entries derived from code inspection, not a real build log; toolchain is
# now available, so this baseline still needs regeneration from a real full
# build log (tracked in grbl/platform/PLAN.md Phase 3). If a CI run reports
# extras, regenerate with:
# python3 ci/warn_ratchet.py --log <log> --emit
hal_gpio.h: warning: "HAL_GPIO_IRQ_HANDLER" redefined
nvmem.c: warning: "EEPROM_SIZE" redefined
system.c: warning: this statement may fall through [-Wimplicit-fallthrough=]
